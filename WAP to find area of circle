#include <stdio.h>

int main() {
    float radius, area;
    const float PI = 3.14159;

    printf("Enter radius: ");
    scanf("%f", &radius);

    area = PI * radius * radius;

    printf("Area of circle = %.2f\n", area); // here %.2 means it will display only two digits after decimal point

    return 0;
}
